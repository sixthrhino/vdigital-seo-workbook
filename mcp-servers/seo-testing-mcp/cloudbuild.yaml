# Submitted from the repo root (`gcloud builds submit . --config=mcp-servers/seo-testing-mcp/cloudbuild.yaml ...`)
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "mcp-servers/seo-testing-mcp/Dockerfile", "-t", "$_IMAGE", "."]
images:
  - "$_IMAGE"
