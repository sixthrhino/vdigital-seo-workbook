# Submitted from the repo root (`gcloud builds submit . --config=agents/seo-assistant/cloudbuild.yaml ...`)
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "agents/seo-assistant/Dockerfile", "-t", "$_IMAGE", "."]
images:
  - "$_IMAGE"
