# Submitted from the repo root (`gcloud builds submit . --config=agents/seo-testing-agent/cloudbuild.yaml ...`)
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "agents/seo-testing-agent/Dockerfile", "-t", "$_IMAGE", "."]
images:
  - "$_IMAGE"
