[project]
name = "seo-workbook-agent"
version = "0.1.0"
description = "ADK-based conversational agent for gathering monthly SEO plans, exposed via a plain HTTP endpoint and a Google Chat webhook."
requires-python = ">=3.11"
# seo-workbook-common isn't listed here since it's not published anywhere —
# resolved via pytest's `pythonpath` setting below rather than an editable
# install (see mcp_server/pyproject.toml for why: editable-install .pth
# resolution was flaky in this environment with multiple locally-path
# packages sharing one venv). The Docker build does a normal non-editable
# `pip install` from a repo-root context instead, which isn't affected.
dependencies = [
    "google-adk>=1.0",
    "mcp>=1.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "google-auth>=2.30",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["seo_workbook_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["../common", "."]
